#ifndef MAIN_H
#define MAIN_H

#define ERROR_STRING	"ERROR_RETURN_VALUE"

enum{
    SHOW_ALL		= (1<<0),
    SHOW_ARRAY_LEN	= (1<<1),
    SHOW_VALUE		= (1<<2),
    SHOW_TYPE		= (1<<3),
    SHOW_ARRAY_STRING	= (1<<4),
};

#endif
